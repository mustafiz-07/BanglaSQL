services:
  banglasql:
    build: .
    container_name: banglasql
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    # Override CMD to keep container alive for interactive use
    command: bash
